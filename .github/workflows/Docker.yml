name: Docker


on:
  workflow_call:

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - run: | 
          echo "Usuário: ${{ vars.USERNAME_DOCKER_HUB }}"

      - name: Docker Login
        uses: docker/login-action@v3.0.0
        with:
          username: ${{ vars.USERNAME_DOCKER_HUB }}
          password: ${{ secrets.PASSWORD_DOCKER_HUB }}
